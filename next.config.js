/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cityedgedevelopment.net'],
    unoptimized: true,
  },
}

module.exports = nextConfig
